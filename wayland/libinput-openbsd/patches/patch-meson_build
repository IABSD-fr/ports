Recognize IABSD as OpenBSD in meson build system checks.

Index: meson.build
--- meson.build.orig
+++ meson.build
@@ -163,7 +163,7 @@
 # Dependencies
 pkgconfig = import('pkgconfig')
 dep_udev = dependency('libudev')
-if host_machine.system() == 'openbsd' or host_machine.system() == 'netbsd'
+if host_machine.system() == 'openbsd' or host_machine.system() == 'iabsd' or host_machine.system() == 'netbsd'
 	dep_libevdev = []
 else
 	dep_libevdev = dependency('libevdev', version: '>= 1.10.0')
@@ -172,7 +172,7 @@
 dep_lm = cc.find_library('m', required : false)
 dep_rt = cc.find_library('rt', required : false)

-if host_machine.system() == 'openbsd' or host_machine.system() == 'netbsd'
+if host_machine.system() == 'openbsd' or host_machine.system() == 'iabsd' or host_machine.system() == 'netbsd'
 	dep_lua = []
 	have_lua = false
 else
@@ -191,7 +191,7 @@
 	config_h.set('HAVE_PLUGINS', 1)
 endif

-if host_machine.system() == 'openbsd' or host_machine.system() == 'netbsd'
+if host_machine.system() == 'openbsd' or host_machine.system() == 'iabsd' or host_machine.system() == 'netbsd'
 	autoload_plugins = false
 else
 	autoload_plugins = get_option('autoload-plugins')
@@ -215,7 +215,7 @@

 ############ mtdev configuration ############

-if host_machine.system() == 'openbsd' or host_machine.system() == 'netbsd'
+if host_machine.system() == 'openbsd' or host_machine.system() == 'iabsd' or host_machine.system() == 'netbsd'
 	have_mtdev = false
 else
 	have_mtdev = get_option('mtdev')
@@ -230,7 +230,7 @@

 ############ libwacom configuration ############

-if host_machine.system() == 'openbsd' or host_machine.system() == 'netbsd'
+if host_machine.system() == 'openbsd' or host_machine.system() == 'iabsd' or host_machine.system() == 'netbsd'
 	have_libwacom = false
 else
 	have_libwacom = get_option('libwacom')
@@ -395,7 +395,7 @@
 		'src/filter-trackpoint.c',
 		'src/filter-trackpoint-flat.c',
 ]
-if host_machine.system() == 'openbsd' or host_machine.system() == 'netbsd'
+if host_machine.system() == 'openbsd' or host_machine.system() == 'iabsd' or host_machine.system() == 'netbsd'
 	dep_libfilter = []
 else
 libfilter = static_library('filter', src_libfilter,
@@ -443,7 +443,7 @@
 config_h.set_quoted('LIBINPUT_PLUGIN_ETCDIR', dir_etc / 'libinput' / 'plugins')

 install_headers('src/libinput.h')
-if host_machine.system() == 'openbsd' or host_machine.system() == 'netbsd'
+if host_machine.system() == 'openbsd' or host_machine.system() == 'iabsd' or host_machine.system() == 'netbsd'
 src_libinput = src_libfilter + [
 	'src/libinput_openbsd.c',
 	'src/libinput-private-config.c',
@@ -533,7 +533,7 @@
 		link_depends : mapfile,
 		install : true
 		)
-if host_machine.system() == 'openbsd'
+if host_machine.system() == 'openbsd' or host_machine.system() == 'iabsd'
 install_headers('include/linux/freebsd/input-event-codes.h', subdir: 'linux/freebsd')
 install_headers('include/linux/freebsd/input.h', subdir: 'linux/freebsd')
