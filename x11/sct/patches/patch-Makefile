--- Makefile.orig	Thu Jun 11 05:45:52 2026
+++ Makefile	Thu Jun 11 05:46:20 2026
@@ -24,6 +24,7 @@
 PREFIX_Darwin=/usr
 PREFIX_Linux=/usr
 PREFIX_OpenBSD=/usr/local
+PREFIX_IABSD=/usr/local
 PREFIX?=$(PREFIX_$(PLATFORM))
 
 BINDIR?=$(PREFIX)/bin
@@ -36,6 +37,7 @@
 MANDIR_Darwin=$(PREFIX)/share/man
 MANDIR_Linux=$(PREFIX)/share/man
 MANDIR_OpenBSD=$(PREFIX)/man
+MANDIR_IABSD=$(PREFIX)/man
 MANDIR?=$(MANDIR_$(PLATFORM))
 
 CFLAGS?=-Os
@@ -44,6 +46,7 @@
 CFLAGS_Darwin=
 CFLAGS_Linux=-fPIC -D_BSD_SOURCE -D_XOPEN_SOURCE=600 -D_GNU_SOURCE
 CFLAGS_OpenBSD=
+CFLAGS_IABSD=
 CFLAGS+=$(CFLAGS_$(PLATFORM))
 
 LDFLAGS+=-L$(LIBDIR) -L/usr/lib -L/usr/X11R6/lib
@@ -52,6 +55,7 @@
 LIBS_Darwin=
 LIBS_Linux=-lrt
 LIBS_OpenBSD=
+LIBS_IABSD=
 LIBS+=$(LIBS_$(PLATFORM))
 
 OBJECTS=sct.o
