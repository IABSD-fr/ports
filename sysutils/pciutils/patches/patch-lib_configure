Add IABSD support to pciutils configure.

Index: lib/configure
--- lib/configure.orig
+++ lib/configure
@@ -30,6 +30,10 @@
 	if [ "$sys" = "DragonFly" ]
 	then
 		sys=freebsd
+	fi
+	if [ "$sys" = "IABSD" ]
+	then
+		sys=openbsd
 	fi
 	if [ "$sys" = "GNU/kFreeBSD" ]
 	then
