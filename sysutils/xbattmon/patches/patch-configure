Add IABSD support.

Index: configure
--- configure.orig
+++ configure
@@ -3,6 +3,9 @@
 case `uname` in
 OpenBSD)
 	ln -sf config.bsd.mk config.mk
+	;;
+IABSD)
+	ln -sf config.bsd.mk config.mk
 	;;
 DragonFly)
 	ln -sf config.dfly.mk config.mk
